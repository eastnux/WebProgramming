import { deviceInfos } from "./charts-env";
import { deviceIds, sensorMapping } from "./devices-env";
import { exampleData, systemParas } from "./project-env";

export const dbParas:any={
deviceIds,
systemParas,
sensorMapping,
deviceInfos,
exampleData
}